{
  "name": "crazycafe",
  "version": "1.0.0",
  "description": "Crazy Café — a wacky drag & stack cooking game where the wackyShooter enemies come for lunch",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
